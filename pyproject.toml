[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "archiver-rag"
version = "0.1.0"
description = "Semantic RAG for Obsidian vaults via MCP"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "chromadb",
    "sentence-transformers",
    "watchdog",
    "mcp",
    "typer",
    "rich",
    "pyyaml",
    "platformdirs",
]

[dependency-groups]
dev = [
    "ruff>=0.16.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
archiver-rag = "archiver_rag.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["archiver_rag"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "vault: requires real vault (opt-in, set ARCHIVER_RAG_VAULT)",
    "slow: loads the sentence-transformers model (embed() calls)",
]
